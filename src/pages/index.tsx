import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
    return (
        <>
            <Head>
                <title>Início</title>
            </Head>

            <div style={{ height: '150vh' }}></div>
        </>
    );
};

export default Home;
